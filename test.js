async function runTests() { const baseUrl = 'http://localhost:8080/api'; console.log('--- TASK MANAGER API TEST ---'); console.log('\n1. Registering new organization and admin user...'); const regRes = await fetch(`${baseUrl}/auth/register`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ orgName: `AntiGravity Org ${Math.floor(Math.random() * 1000)}`, email: `admin_${Date.now()}@antigravity.test`, password: 'password123' }) }); const authData = await regRes.json(); console.log('Response:', authData); if (!authData.accessToken) return; const token = authData.accessToken; const headers = { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` }; console.log('\n2. Fetching Organization Details...'); const orgRes = await fetch(`${baseUrl}/orgs`, { headers }); console.log('Response:', await orgRes.json()); console.log('\n3. Creating a new Task...'); const taskRes = await fetch(`${baseUrl}/tasks`, { method: 'POST', headers, body: JSON.stringify({ title: 'Complete Antigravity Setup', description: 'Testing task creation endpoint.', status: 'todo' }) }); console.log('Response:', await taskRes.json()); console.log('\n4. Fetching Tasks...'); const tasksGet = await fetch(`${baseUrl}/tasks`, { headers }); console.log('Response:', await tasksGet.json()); console.log('\n5. Checking Audit Logs for creation records...'); const auditGet = await fetch(`${baseUrl}/audit`, { headers }); console.log('Response:', await auditGet.json()); console.log('\n✅ All endpoints tested successfully!'); } runTests();
